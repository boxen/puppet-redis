# Let the env know where Redis runs.

export BOXEN_REDIS_HOST=<%= @final_config['host] %>
export BOXEN_REDIS_PORT=<%= @final_config['port'] %>

export BOXEN_REDIS_URL="redis://$BOXEN_REDIS_HOST:$BOXEN_REDIS_PORT/"
